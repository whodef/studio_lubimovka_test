import './Header.css';

const Header = () => {
    return(
        <header className="header">
            <h1 className="app-title">По запросу «август» мы нашли</h1>
        </header>
    );
}

export default Header;